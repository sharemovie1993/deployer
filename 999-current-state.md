# CURRENT STATE

Completed:
- **GUI Wizard Setup Wizard**: Pembuat langkah-demi-langkah interaktif berbasis web (6 Tahap) untuk setup Absenta.
- **Hidden Window Launcher**: Berkas batch `Mulai Installer Absenta.bat` menggunakan VBScript untuk inisialisasi server local latar belakang secara senyap.
- **Remote Linux Deployer**: Skrip `deploy-absenta-remote.ps1` untuk orkestrasi penuh instalasi stack server Linux.
- **Windows On-Premise Deployer**: Skrip `deploy-onprem-windows.ps1` untuk setup intranet sekolah berbasis Windows.
- **Easy Swap Scale Utility**: Skrip otomatisasi pembuatan dan penskalaan ukuran memori virtual swap (`easy-swap.ps1` & `setup-swap.sh`).
- **Easy Disk Resizer**: Otomatisasi pembesaran ukuran partisi disk (`easy-resize.ps1` & `setup-resize.sh`).
- **Easy SSH Key Provisioner**: Otomatisasi pendaftaran public key ke server target (`easy-setup-ssh.ps1` & `setup-ssh.sh`).
- **Easy DB Migrator**: Skrip otomatisasi sinkronisasi skema database Postgres (`easy-migrate.ps1`).
- **Easy Log Purger**: Skrip pembersihan cache dan log sampah server harian (`easy-purge.ps1`).

In Progress:
- None

Current Focus:
- Documentation and final deployment checks.

Next Task:
- Packaging the installer directory for school operators distribution.
